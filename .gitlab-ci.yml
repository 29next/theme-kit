image: ubuntu:20.04

stages:
    - test

test:
    stage: test
    script:
        - apt update && apt install python3-pip
        - pip3 install .[test]
        - flake8
        - python setup.py test
