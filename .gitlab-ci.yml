image: ubuntu:20.04

stages:
    - test

test:
    stage: test
    script:
        - apt -y update && apt -y install python3-pip
        - apt install python3.7
        - pip install .[test]
        - flake8
        - tox
